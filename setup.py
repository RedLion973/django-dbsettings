from distutils.core import setup

setup(name='dbsettings',
      version='1.0.1',
      description='django-values to set persisted values as options for your django projects, apps or models',
      author='RedLion973',
      packages=['dbsettings'],
     )
